- **Auto-approve tool permissions** — a new "Tool permissions" mode in **Tools → Options → AI Flow** lets the agent run without the permission pop-ups: *Ask every time* (default), *Auto-approve edits, ask before destructive*, or *Auto-approve everything*. Works for both Chat and Terminal; auto-approvals are still recorded in the audit log.
- **Cleaner AI Flow options page** — reorganized into clear sections (Tool permissions, Agent edits, IDE behavior, Diagnostics), one line per setting.
- Carried from 0.24.x: per-command intent guard (RULE #1), live-buffer code reads + `GetPendingReviews`, handler-body review diff, FMX wiring seams.
