- **Issue reporting is now opt-in** — a new **Issue reporting** toggle in **Tools → Options → Aefos → AI Flow** controls whether the agent's bug/suggestion dialog (`ProposeAefosIssue`) can open. **Off by default**: turn it on when you want to send feedback, and the agent can never pop the dialog otherwise.
- Includes everything from 0.25.0: built-in project templates (Console/VCL/FMX/Library/Package/DUnitX), DUnitX ready-to-run scaffolding, and the agent issue-report flow.
