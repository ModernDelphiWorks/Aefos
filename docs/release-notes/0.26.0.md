- **Manage your Python tools from the IDE (Pro)** — a new **Aefos PyTools** item in the IDE **View** menu opens a manager to **create, edit and delete** the Python tools the agent can use (in both Chat and Terminal) — no more hand-editing files. Each tool is still a folder under `%APPDATA%\Aefos\pytools` with a `tool.json` + `main.py`; the manager just writes them for you. New tools load on the next MCP session.
- Carried from 0.25.x: built-in project templates (Console/VCL/FMX/Library/Package/DUnitX), DUnitX ready-to-run scaffolding, the agent issue-report flow (opt-in via AI Flow), and auto-approve tool permissions.
